- title "Enquiry"

-@enquiry.enquiry_fields.each do |field|
  ="#{field.name.humanize}:"
  %br
  =field.value
  %br
  %br
Sent on:
%br
=@enquiry.created_at
%br
  
  
-options_panel do
  =back_link
  = link_to "Destroy", @enquiry, :confirm => 'Are you sure?', :method => :delete
  = link_to "View All", enquiries_path
